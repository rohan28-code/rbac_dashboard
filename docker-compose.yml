services:
  web:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      - PORT=${PORT:-8000}
    volumes:
      - db_data:/app/data
    restart: always

volumes:
  db_data:
